import {Entities, Entity, Tuid, Action, Sheet, Query} from '../entities';
import {EntitiesUI} from './entitiesUI';
import {EntityUIO} from './entityUI';

export class ActionUI extends EntityUIO<Action> {
}
